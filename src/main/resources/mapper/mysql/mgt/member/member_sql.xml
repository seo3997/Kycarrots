<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mgt.member">	

	<select id="selectId" parameterType="dataMap" resultType="DataMap">
		SELECT user_id 
			FROM op_user 
			WHERE 1 = 1
			<if test="member_phone!= null and member_phone != ''">
			  AND user_nm = #{member_name}
			  AND cttpc   = #{member_phone}
			</if>
			<if test="member_email != null and member_email != ''">
				AND email=#{member_email}
			</if>
	</select>
	
	<select id="selectPw" parameterType="dataMap" resultType="DataMap">
		SELECT password as user_pw
		FROM op_user
		WHERE user_id = #{member_id}
	</select>
	

	<update id="updatePw" parameterType="dataMap">
		UPDATE op_user 
		SET PASSWORD = #{member_new_pw}
	    WHERE user_ID= #{member_id}
	</update>
	
</mapper>